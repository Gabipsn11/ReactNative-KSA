import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  imageContainer: {
    marginTop: '10%',
    height: 195,
    marginLeft: '-40%',
  },
  image: {
    width: '100%',
    height: '100%',
  },
});
